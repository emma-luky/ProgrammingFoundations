# module: python file that contains code, like variables or functions
# package/library: using multiple modules together so they are distributed and used in a group
# framework: when a set of code is not just used together but used in a specifc way

def mult(x, y):
    print(f'{x} * {y} = {x*y}')